html
  head
    title= title
  body
    h1= message
    form(method='POST' action='/api/genres')
     div.form-group
       label(for='name') Name:
       input#name.form-control(type='text', placeholder='first and last' name='name')
       button.btn.btn-primary(type='submit') Add Genre
